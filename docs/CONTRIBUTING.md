# Contributing

Keep changes focused and add tests for payment, authorization, or reconciliation
behavior. Never commit environment files, signed XDR, secret keys, provider
credentials, access tokens, or customer records.

Run unit tests, lint, and the production build. Contract changes require
Soroban tests and updated deployment evidence after a verified deployment.
