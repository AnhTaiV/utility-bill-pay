# Reconciliation

Compare each bill's local status with Horizon settlement, Soroban proof, and
biller-provider state. Queue mismatches instead of guessing.

Review settled-but-unsubmitted bills, provider success without local completion,
amount or reference mismatches, and duplicate provider receipts. Changes to
local status require durable evidence.
