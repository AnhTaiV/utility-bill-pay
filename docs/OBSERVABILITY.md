# Observability

Measure bill creation, prepare and confirm outcomes, Horizon latency, provider
latency, reconciliation backlog, and duplicate-attempt prevention. Logs should
use bill IDs and transaction hashes, not customer account numbers.

Alert on paid bills awaiting provider confirmation and on unexpected changes to
the configured network, issuer, or settlement recipient.
